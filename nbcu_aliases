## Places
alias @cpc='cd $WORKPLACE/cp-config-repo'
alias cpc='cd $WORKPLACE/cp-config-repo'
alias @dg='cd $WORKPLACE/digsrv-config-repo'
alias @toon='cd $WORKPLACE/cp-transfer-s3toonprem'
alias @ds='cd $WORKPLACE/cp-transfer-upload-s3'
alias @s3s3='cd $WORKPLACE/cp-transfer-upload-s3'
alias @s3='cd $WORKPLACE/cp-transfer-upload-s3'
alias @ots='cd $WORKPLACE/cp-transfer-upload'
alias @mcp='cd $WORKPLACE/digsrv-mcp-extractor'
alias @diva='cd $WORKPLACE/cp-transfer-diva-restore'
alias @tools='cd $WORKPLACE/cp-tools'
alias @mpx='cd $WORKPLACE/cp-mpx-ingest'
alias @delivery='cd $WORKPLACE/cp-delivery'

function @config {
    declare -A exceptions
    exceptions[digsrv-cds-ingest-mpx]="cds-ingest-mpx"
    exceptions[digsrv-cds-ingest-mpx-sqe]="cds-ingest-qa"
    exceptions[cp-manatees-performance-tests]="cp-transfer-performance-tests"
    [ $1 ] && {
        [[ $1 = -* ]] && local suffix=$1 || local suffix="-$1"
    }
    [[ $(pwd) = $WORKPLACE/output/* ]] && {
        cd $WORKPLACE/*-config-repo/$(basename $(pwd))$suffix
        return
    }
    [[ $(pwd) = $WORKPLACE/*-config-repo/*-sqe ]] && {
        cd ../$(basename $(pwd) | sed -e 's/-sqe//g')$suffix
        return
    }
    [[ $(pwd) = $WORKPLACE/*-config-repo/* ]] && {
        cd ../$(basename $(pwd))$suffix
        return
    }
    [[ $(pwd) = $WORKPLACE/* ]] && {
        local repoName=$(basename $(git rev-parse --show-toplevel))
        local configProjectName=${${exceptions[$repoName]}:-$repoName}
        cd $WORKPLACE/*-config-repo/$configProjectName$suffix
        return
    }
    [[ $(pwd) = $TASK_HOME/* ]] && {
        local repoName=$(cat $(git rev-parse --show-toplevel)/.yagconf)
        local configProjectName=${${exceptions[$repoName]}:-$repoName}
        cd $WORKPLACE/*-config-repo/$configProjectName$suffix
        return
    }
    echo "Not sure where the config is?"
    return 1
}

function jbcp() {
    @@
    local link="https://jira.inbcu.com/browse/$(basename $(dirname $(pwd)))-$(basename $(pwd))"
    echo $link | pbcopy
    echo "JIRA link copied to clipboard ($link)"

    popd
}

