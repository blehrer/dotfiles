git:
  autoCommit: true
  autoPush: true
edit:
  watch: true
diff:
  command: delta
  pager: diff-so-fancy
merge:
  command: nvim
  args:
    - -d
    - "{{ .Destination }}"
    - "{{ .Source }}"
    - "{{ .Target }}"
