git:
  autoAdd: true
  autoCommit: true
  autoPush: true
edit:
  watch: true
diff:
  pager: bat
merge:
  command: nvim
  args:
    - -d
    - "{{ .Destination }}"
    - "{{ .Source }}"
    - "{{ .Target }}"
