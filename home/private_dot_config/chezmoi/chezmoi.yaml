git:
  autoAdd: true
  autoCommit: true
  autoPush: true
edit:
  watch: true
diff:
  command: delta
merge:
  command: nvim
  args:
    - -d
    - "{{ .Destination }}"
    - "{{ .Source }}"
    - "{{ .Target }}"
